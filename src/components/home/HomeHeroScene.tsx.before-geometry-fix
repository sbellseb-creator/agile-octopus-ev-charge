import {
  BatteryCharging,
  CloudRain,
  CloudSnow,
  CloudSun,
  Moon,
  PlugZap,
  Sparkles,
  Sun,
} from "lucide-react";

import type { HomeScene } from "@/lib/home-scene";
import {
  homeCarAsset,
  homeSceneBackground,
} from "@/lib/home-scene-assets";

interface Props {
  scene: HomeScene;
  charging: boolean;
  pluggedIn?: boolean;
  batteryLevel?: number | null;
  chargeLimit?: number | null;
  chargerPowerKw?: number | null;
  state?: string | null;
}

function WeatherIcon({ scene }: { scene: HomeScene }) {
  if (scene.weather === "snow") {
    return <CloudSnow className="h-4 w-4" />;
  }

  if (
    scene.weather === "rain" ||
    scene.weather === "storm"
  ) {
    return <CloudRain className="h-4 w-4" />;
  }

  if (scene.phase === "night") {
    return <Moon className="h-4 w-4" />;
  }

  if (scene.weather === "clear") {
    return <Sun className="h-4 w-4" />;
  }

  return <CloudSun className="h-4 w-4" />;
}

export default function HomeHeroScene({
  scene,
  charging,
  pluggedIn = false,
  batteryLevel,
  chargeLimit,
  chargerPowerKw,
  state,
}: Props) {
  const battery =
    batteryLevel != null
      ? `${Math.round(batteryLevel)}%`
      : "—";

  const background = homeSceneBackground(scene);
  const car = homeCarAsset();

  return (
    <div className="relative min-h-[380px] overflow-hidden rounded-[30px] bg-slate-950">

      {/* Real/generated weather scene */}
      <div
        className="absolute inset-0 bg-cover bg-center transition-all duration-700"
        style={{
          backgroundImage: `url("${background}")`,
        }}
      />

      {/* Fallback atmosphere while an image is unavailable */}
      <div className="absolute inset-0 bg-gradient-to-br from-slate-800 via-slate-900 to-slate-950 -z-10" />

      {/* Premium cinematic grading */}
      <div className="absolute inset-0 bg-gradient-to-t from-black/75 via-black/10 to-black/10" />
      <div className="absolute inset-0 bg-gradient-to-r from-black/35 via-transparent to-black/10" />

      {/* Top status */}
      <div className="absolute left-4 top-4 z-30 rounded-2xl border border-white/15 bg-black/35 px-4 py-3 shadow-2xl backdrop-blur-xl">
        <div className="flex items-center gap-2">
          <BatteryCharging
            className={
              charging
                ? "h-5 w-5 text-emerald-300"
                : "h-5 w-5 text-white/80"
            }
          />

          <span className="text-3xl font-black tracking-tight text-white">
            {battery}
          </span>
        </div>

        <div className="mt-1 text-xs font-semibold text-white/85">
          {charging
            ? `${chargerPowerKw != null
                ? `${chargerPowerKw.toFixed(1)} kW · `
                : ""
              }Charging`
            : state || "Last known status"}
        </div>

        {chargeLimit != null && (
          <div className="mt-1 text-[10px] text-white/65">
            Target {Math.round(chargeLimit)}%
          </div>
        )}
      </div>

      {/* Weather badge */}
      <div className="absolute right-4 top-4 z-30 flex items-center gap-2 rounded-full border border-white/15 bg-black/30 px-3 py-2 text-xs text-white/85 backdrop-blur-xl">
        <WeatherIcon scene={scene} />

        <span className="capitalize">
          {scene.mode === "forced"
            ? scene.theme
            : scene.weather.replace("-", " ")}
        </span>

        {scene.temperatureC != null && (
          <span className="font-bold">
            {Math.round(scene.temperatureC)}°
          </span>
        )}
      </div>

      {/* Quicksilver vehicle layer */}
      <img
        src={car}
        alt="Quicksilver Model Y"
        className="absolute bottom-[34px] left-1/2 z-20 w-[78%] max-w-[690px] -translate-x-1/2 object-contain drop-shadow-[0_26px_28px_rgba(0,0,0,.55)]"
      />

      {/* Dynamic Tesla charger state */}
      <div
        className={[
          "pointer-events-none absolute left-[25.4%] top-[43%] z-30 h-[30px] w-[8px] rounded-full transition-all duration-500",
          charging
            ? "bg-emerald-300 shadow-[0_0_15px_5px_rgba(52,211,153,.7)] animate-pulse"
            : pluggedIn
              ? "bg-blue-400 shadow-[0_0_10px_3px_rgba(96,165,250,.5)]"
              : "bg-white/90 shadow-[0_0_8px_2px_rgba(255,255,255,.35)]",
        ].join(" ")}
      />

      {(pluggedIn || charging) && (
        <svg
          viewBox="0 0 1000 500"
          preserveAspectRatio="none"
          className="pointer-events-none absolute inset-0 z-[25] h-full w-full overflow-visible"
          aria-hidden="true"
        >
          {/* Physical black charging lead */}
          <path
            d="M 265 245
               C 260 310, 245 365, 300 410
               C 370 465, 500 448, 560 385
               C 586 357, 590 322, 606 292"
            fill="none"
            stroke="rgba(14,18,24,.96)"
            strokeWidth="9"
            strokeLinecap="round"
            strokeLinejoin="round"
          />

          {/* Subtle state illumination on the real cable */}
          <path
            d="M 265 245
               C 260 310, 245 365, 300 410
               C 370 465, 500 448, 560 385
               C 586 357, 590 322, 606 292"
            fill="none"
            stroke={charging ? "rgba(52,211,153,.68)" : "rgba(96,165,250,.58)"}
            strokeWidth="3.5"
            strokeLinecap="round"
            strokeLinejoin="round"
          />

          {/* Travelling energy pulse only while actively charging */}
          {charging && (
            <path
              d="M 265 245
                 C 260 310, 245 365, 300 410
                 C 370 465, 500 448, 560 385
                 C 586 357, 590 322, 606 292"
              fill="none"
              stroke="rgba(110,255,190,.98)"
              strokeWidth="4"
              strokeLinecap="round"
              strokeDasharray="22 105"
              className="ev-charge-flow"
            />
          )}
        </svg>
      )}

      {/* Tesla rear-left charge-port state */}
      {(pluggedIn || charging) && (
        <div
          className={[
            "pointer-events-none absolute left-[59.5%] top-[55%] z-30 h-4 w-4 rounded-full transition-all duration-500",
            charging
              ? "bg-emerald-300 shadow-[0_0_18px_7px_rgba(52,211,153,.75)] animate-pulse"
              : "bg-blue-400 shadow-[0_0_12px_4px_rgba(96,165,250,.5)]",
          ].join(" ")}
        />
      )}

      {charging && (
        <div className="absolute bottom-4 left-1/2 z-40 -translate-x-1/2 rounded-full border border-emerald-300/30 bg-emerald-950/75 px-5 py-2 text-xs font-black tracking-[0.13em] text-emerald-100 shadow-[0_0_22px_rgba(52,211,153,.28)] backdrop-blur-xl">
          ⚡ LIVE CHARGING
        </div>
      )}

      {/* Forced theme sparkle */}
      {scene.mode === "forced" && (
        <Sparkles className="absolute bottom-5 left-5 z-30 h-5 w-5 text-white/70" />
      )}
    </div>
  );
}
